services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: local-ai-model-scraper
    ports:
      - "3001:80"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
